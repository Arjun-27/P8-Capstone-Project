<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/item_hybrid"
          android:title="@string/hybrid"
          app:showAsAction="never" />

    <item android:id="@+id/item_satellite"
          android:title="@string/satellite"
          app:showAsAction="never" />

    <item android:id="@+id/item_terrain"
          android:title="@string/terrain"
          app:showAsAction="never" />

    <item android:id="@+id/item_normal"
          android:title="@string/normal"
          app:showAsAction="never" />
</menu>